{
  "name": "eko",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev:desktop": "PLATFORM=desktop vite",
    "dev:mobile": "PLATFORM=mobile vite",
    "dev": "PLATFORM=desktop vite",
    "build:desktop": "tsc --noEmit && PLATFORM=desktop vite build",
    "build:mobile": "tsc --noEmit && PLATFORM=mobile vite build",
    "build": "tsc --noEmit && PLATFORM=desktop vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "lint": "biome check",
    "lint:fix": "biome check --write",
    "format": "biome format --write"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "@tailwindcss/vite": "^4.3.0",
    "@tauri-apps/cli": "^2",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^4.6.0",
    "tailwindcss": "^4.3.0",
    "typescript": "~5.8.3",
    "vite": "^7.0.4"
  }
}
